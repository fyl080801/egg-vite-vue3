datasource:

listeners:

model:
  text: ''

fields:
  - component: el-form
    row:
      gutter: 20
    props:
      labelWidth: 120px
    children:
      - component: el-input
        form:
          label: 输入
        col:
          span: 6
        model: model.text

      - component: el-input
        form:
          label: 输入
        col:
          span: 6
        model: model.text
